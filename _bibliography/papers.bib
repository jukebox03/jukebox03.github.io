---
---

@comment{
  Publications go here, in BibTeX format. jekyll-scholar renders them on /publications/.
  Mark an entry with `selected={true}` to also surface it on the front page
  (requires `selected_papers: true` in _pages/about.md).
}
