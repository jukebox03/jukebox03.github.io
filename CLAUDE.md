# CLAUDE.md

@AGENTS.md

`AGENTS.md` is the authoritative guide for this repo. Read it before editing — in particular the `baseurl` warning, which is a trap inherited from upstream al-folio.

Upstream's own `docs/` directory is still present and is a useful reference (`docs/CUSTOMIZE.md`, `docs/FAQ.md`), but it is written for the al-folio project, not for this site. Where the two disagree, `AGENTS.md` wins.
